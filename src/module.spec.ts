import 'mocha'
import { expect } from 'chai'
import * as path from 'path'

import { ComponentFixture } from './'

describe('test kio-ng2-component-routing',()=>{

  describe('exports interfaces',()=>{

    xit('ComponentFixture',()=>{
      //expect(ComponentFixture).to.include.key('ComponentFixture')
    })

  })

})